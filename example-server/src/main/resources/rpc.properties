rpc.zookeeper.address=127.0.0.1:2181
# Set this explicitly when automatic host detection selects a virtual or unreachable interface.
rpc.server.host=127.0.0.1
# Bind independently from the address advertised to clients.
rpc.server.bind-host=0.0.0.0
